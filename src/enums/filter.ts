export enum ProductTier {
  Basic = "Basic",
  Premium = "Premium",
}

export enum ProductTheme {
  Dark = "Dark",
  Light = "Light",
}

export enum SortType {
  Ascending = "asc",
  Descending = "desc",
}
